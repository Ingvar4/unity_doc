---
title: Скрипт UITextManager
description: Чтобы управлять этими тремя новыми полями TextMeshPro, мы добавим еще один простой скрипт.
---

1. Создайте новый скрипт с именем UTIMext anager.
2. Переместите UITextManager в папку *__Scripts*.
3. Откройте UITextManager в Visual Studio и введите код, в листинге.

      Листинг кода UITextManager.cs
```c#
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class UITextManager : MonoBehaviour {
    private static UITextManager S;

    [Header( "Inscribed" )]
    public TMP_Text gameOverText;
    public TMP_Text roundResultText;
    public TMP_Text highScoreText;

    [Header( "Dynamic" )]
    [SerializeField]
    private bool _resultsUIFieldsVisible = false;

    public bool resultsUIFieldsVisible {
        get { return _resultsUIFieldsVisible; }
            private set {
            _resultsUIFieldsVisible = value;
            gameOverText.gameObject.SetActive( value );
            roundResultText.gameObject.SetActive( value );
        }
    }

    void Start() {
        if (S != null) Debug.LogWarning("Attempt to set Singleton S again!");
        S = this;

        ShowHighScore();
        resultsUIFieldsVisible = false;
    }

    void ShowHighScore() {
        // The 0 in "#,##0" on the following line is a zero.
        string str = $"High Score: {ScoreManager.HIGH_SCORE:#,##0}";
        highScoreText.text = str;
    }

    /// <summary>
    /// Static wrapper for S.GameOverUI(). Called when the game is over.
    /// </summary>
    /// <param name="won">True if the player won.</param>
    static public void GAME_OVER_UI( bool won ) {
        S.GameOverUI( won );
    }

    /// <summary>
    /// Called when the game is over.
    /// </summary>
    /// <param name="won">True if the player won.</param>
    public void GameOverUI( bool won ) {
        int score = ScoreManager.SCORE;
        string str;
        if ( won ) {
            gameOverText.text = "Round Over";
            str = "You won this round!\n"
            + $"Round Score: {ScoreManager.SCORE_THIS_ROUND:#,##0}";
        } else {
            gameOverText.text = "Game Over";
            if ( ScoreManager.HIGH_SCORE <= score ) {
                str = $"You got the high score!\nHigh score: {score:#,##0}";
            } else {
                str = $"Your final score was:\n{score:#,##0}";
            }
        }
        roundResultText.text = str;
        resultsUIFieldsVisible = true;
        ShowHighScore(); // Update the high score shown at the end of each round.
    }

    // void Update() {…} // Don’t forget to delete Update()
}
```

4. Откройте скрипт Prospector в Visual Studio и добавьте одну строку из листинга кода в конец
метода GameOver. Это заставит Prospector вызвать
`UITextManager.GAME_OVER_UI()` с переменной bool, определяющей, выиграл игрок или проиграл.  

      Листинг кода  Prospector.cs
```c#
public class Prospector : MonoBehaviour {
    …
    void GameOver( bool won ) {
        …
        // But wait a moment first, giving the final score a moment to travel
        Invoke ("ReloadLevel", roundDelay);
        // SceneManager.LoadScene("__Prospector_Scene_0");

        UITextManager.GAME_OVER_UI(won);
    }
}
```

5. Сохраните все скрипты в Visual Studio и вернитесь в Unity.
6. В Unity прикрепите UITex tManager к MainCamera в иерархии.
7. В компоненте UITex tManager (Script) объекта _MainCamera присвойте
полям GameOver, RoundResult и HighScore TextMeshPro Text
соответствующие публичные поля UITex tManager.  

Теперь, когда вы завершаете раунд или игру, вы должны увидеть
сообщения, подобные тем, что показаны на рисунке.

<img src="/unity_doc/images/ex125.jpeg" alt="1" style={{  height: 'auto' }} />

Примеры сообщений об окончании игры после
четырех раундов игры с подсчетом лучших результатов